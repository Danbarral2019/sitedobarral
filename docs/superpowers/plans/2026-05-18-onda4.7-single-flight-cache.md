# Onda 4.7 — Single-flight em `withCache` Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar single-flight de-duplication in-memory ao helper `withCache` em `lib/cache/redis-client.ts`, eliminando cache stampede dentro de cada lambda. Default-on automático nos 24 call sites; escape hatch `singleFlight: false` disponível.

**Architecture:** Uma constante de módulo `inFlight: Map<string, Promise<unknown>>` no `lib/cache/redis-client.ts`. `withCache` consulta o Map antes de executar `fn()`; se há promise registrada, retorna ela (compartilhada). `try/finally` garante `inFlight.delete(key)` em sucesso ou erro. Sem distributed lock — implementação per-process.

**Tech Stack:** Next.js 15 • TypeScript 5 • Upstash Redis (`@upstash/redis`) • Vitest 4 (`// @vitest-environment node` para tests server-side) • Pino logger (`@/lib/logger`) • Sentry (`@sentry/nextjs`)

**Spec:** [docs/superpowers/specs/2026-05-18-onda4.7-single-flight-cache-design.md](../specs/2026-05-18-onda4.7-single-flight-cache-design.md)

---

## File Structure

**Arquivos criados nesta implementação:**

| Arquivo | PR | Responsabilidade |
|---|---|---|
| `lib/cache/__tests__/single-flight.test.ts` | 4.7 | Cobertura dedicada do behavior single-flight (separado do `redis-client.test.ts` existente para isolar dependências) |

**Arquivos modificados:**

| Arquivo | PR | Mudança |
|---|---|---|
| `lib/cache/redis-client.ts` | 4.7 | Adiciona `inFlight` Map de módulo; refatora `withCache` com single-flight; estende `WithCacheOptions`; exporta `__resetInFlightForTesting` |
| `lib/cache/redis-client.ts` | 4.7.1 | Adiciona `MAX_IN_FLIGHT` + `registerInFlight` helper com warning amostrado |
| `lib/cache/__tests__/single-flight.test.ts` | 4.7.1 | Adiciona 1 teste cobrindo high-water-mark warning |

**Arquivos NÃO modificados:** Nenhum dos 24 call sites de `withCache` precisa ser tocado. Behavior é default-on.

---

## Convenções

- **Path:** comandos rodam de `/Users/danba/Site do Barral/sitedobarral/`
- **Testes server-side:** docblock `// @vitest-environment node` na primeira linha
- **Mock Upstash Redis:** padrão `vi.hoisted` (ver `lib/cache/__tests__/redis-client.test.ts` linhas 24-49 como template)
- **Mock logger:** `vi.mock('@/lib/logger', () => ({ apiLogger: { info: vi.fn(), error: vi.fn(), warn: vi.fn(), debug: vi.fn() } }))`
- **Mock Sentry:** `vi.mock('@sentry/nextjs', () => ({ captureMessage: vi.fn(), addBreadcrumb: vi.fn() }))`
- **Commit messages:** padrão Onda 4 — `<tipo>(cache): <descrição> [Onda 4 PR 4.7.X]`
- **Co-Authored-By:** `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>`
- **Git author/committer (CRÍTICO):** SEMPRE Daniel Barral <danbarral@gmail.com>. Forma exata:
  ```bash
  git -c user.email=danbarral@gmail.com -c user.name="Daniel Barral" commit -m "..."
  ```
  NUNCA modificar git config. NUNCA push até a task de push explícita.
- **Branches:** `refactor/cache-single-flight` (PR 4.7), `feat/cache-leak-protection` (PR 4.7.1, criada de main pós-merge)

---

## PR 4.7 — Foundation: single-flight in-memory

Branch: `refactor/cache-single-flight`

### Task 1: Criar branch + commit do spec/plan

**Files:**
- Already exist on disk (untracked):
  - `docs/superpowers/specs/2026-05-18-onda4.7-single-flight-cache-design.md`
  - `docs/superpowers/plans/2026-05-18-onda4.7-single-flight-cache.md`

- [ ] **Step 1: Garantir main atualizado**

```bash
git status --short
git checkout main 2>/dev/null
git pull origin main
```

Expected: working tree clean (exceto pelos 2 docs untracked); main na ponta do remote.

- [ ] **Step 2: Criar branch**

```bash
git checkout -b refactor/cache-single-flight
```

- [ ] **Step 3: Commit dos docs**

```bash
git add docs/superpowers/specs/2026-05-18-onda4.7-single-flight-cache-design.md docs/superpowers/plans/2026-05-18-onda4.7-single-flight-cache.md
git -c user.email=danbarral@gmail.com -c user.name="Daniel Barral" commit -m "$(cat <<'EOF'
docs(cache): spec + plan da Onda 4.7 single-flight [Onda 4 PR 4.7]

Foundation documental: single-flight de-duplication in-memory
para withCache, eliminando cache stampede dentro de cada lambda.
Default-on automático nos 24 call sites; escape hatch disponível.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 4: Verificar autoria**

```bash
git log -1 --format='%h | A:%ae | C:%ce | %s'
```

Expected: `A:danbarral@gmail.com | C:danbarral@gmail.com`. Se diferente, `git commit --amend --reset-author` usando os flags `-c` correctos.

---

### Task 2: Criar test file scaffold + 1º teste failing (single caller)

**Files:**
- Create: `lib/cache/__tests__/single-flight.test.ts`

- [ ] **Step 1: Criar arquivo de testes com setup completo + 1º teste**

Criar `lib/cache/__tests__/single-flight.test.ts`:

```ts
// @vitest-environment node
/**
 * Testes para single-flight de-duplication em withCache.
 * Cobre dedup in-memory de chamadas concorrentes para a mesma key.
 */

import { describe, it, expect, vi, beforeEach } from 'vitest';

vi.hoisted(() => {
  process.env.UPSTASH_REDIS_REST_URL = 'https://test-redis.upstash.io';
  process.env.UPSTASH_REDIS_REST_TOKEN = 'test-token-123';
});

const mockGet = vi.hoisted(() => vi.fn());
const mockSetex = vi.hoisted(() => vi.fn());

vi.mock('@upstash/redis', () => ({
  Redis: class MockRedis {
    get = mockGet;
    setex = mockSetex;
    // Métodos não usados nestes testes mas chamados internamente — stubs no-op
    sadd = vi.fn();
    smembers = vi.fn();
    del = vi.fn();
    exists = vi.fn();
    incr = vi.fn();
    expire = vi.fn();
    ping = vi.fn();
  },
}));

vi.mock('@/lib/logger', () => ({
  apiLogger: {
    info: vi.fn(),
    error: vi.fn(),
    warn: vi.fn(),
    debug: vi.fn(),
  },
}));

vi.mock('@sentry/nextjs', () => ({
  captureMessage: vi.fn(),
  addBreadcrumb: vi.fn(),
}));

vi.spyOn(console, 'log').mockImplementation(() => {});
vi.spyOn(console, 'warn').mockImplementation(() => {});

import { withCache, __resetInFlightForTesting } from '@/lib/cache/redis-client';

describe('withCache single-flight', () => {
  beforeEach(() => {
    __resetInFlightForTesting();
    vi.clearAllMocks();
    mockGet.mockResolvedValue(null); // default: cache miss
    mockSetex.mockResolvedValue('OK');
  });

  it('caller único executa fn() uma vez e retorna o resultado', async () => {
    const fn = vi.fn(async () => 42);
    const result = await withCache('test:single', fn, 60);

    expect(result).toBe(42);
    expect(fn).toHaveBeenCalledTimes(1);
    expect(mockSetex).toHaveBeenCalledWith('test:single', 60, 42);
  });
});
```

- [ ] **Step 2: Rodar teste para confirmar FAIL**

Run: `npx vitest run lib/cache/__tests__/single-flight.test.ts`
Expected: FAIL com `Cannot find module '@/lib/cache/redis-client'` exportando `__resetInFlightForTesting`. (O export ainda não existe.)

- [ ] **Step 3: Commit do teste failing (TDD red)**

```bash
git add lib/cache/__tests__/single-flight.test.ts
git -c user.email=danbarral@gmail.com -c user.name="Daniel Barral" commit -m "$(cat <<'EOF'
test(cache): scaffold + first failing test for single-flight [Onda 4 PR 4.7]

TDD red. Importa __resetInFlightForTesting que ainda não existe.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: Implementar single-flight básico + __resetInFlightForTesting

**Files:**
- Modify: `lib/cache/redis-client.ts`

- [ ] **Step 1: Adicionar `inFlight` Map no escopo do módulo**

Editar `lib/cache/redis-client.ts`. Localizar a seção antes de `export interface WithCacheOptions` (~linha 660). Adicionar logo antes:

```ts
// ===========================
// Single-flight de-duplication (in-memory, per-process)
// ===========================

/**
 * Mapa de promises em andamento para de-duplicar chamadas concorrentes
 * com a mesma key. Vive no escopo do módulo (per-process).
 *
 * Quando 2+ callers ativam `withCache` para a mesma key durante um cache miss,
 * apenas o primeiro registra a promise e executa `fn()`; os demais recebem
 * a mesma promise via `inFlight.get(key)` e compartilham o resultado.
 *
 * O Map é limpo via `try/finally` na função wrappper. Não polui o cache em erro.
 */
const inFlight = new Map<string, Promise<unknown>>();

/**
 * @internal — usado apenas por testes. Limpa o registry de promises
 * em andamento entre casos de teste.
 */
export function __resetInFlightForTesting(): void {
  inFlight.clear();
}
```

- [ ] **Step 2: Estender `WithCacheOptions`**

Localizar `export interface WithCacheOptions` (linha ~661 atual). Substituir o bloco completo por:

```ts
export interface WithCacheOptions {
  /** Prefix for registry (enables prefix-based invalidation) */
  prefix?: string;

  /**
   * Default: true. Single-flight de-duplica chamadas concorrentes para a
   * mesma key dentro deste processo. Quando 2+ requests disparam cache miss
   * para a mesma key simultaneamente, apenas uma roda `fn()`; as demais
   * aguardam a mesma Promise e compartilham o resultado.
   *
   * Defina como false para optar por NÃO dedupar (raro — nenhum caller atual
   * precisa). Útil apenas se `fn()` tiver side effects que devem ocorrer N vezes.
   */
  singleFlight?: boolean;
}
```

- [ ] **Step 3: Refatorar `withCache` com single-flight**

Localizar `export async function withCache<T>` (linha ~675 atual). Substituir o corpo inteiro:

```ts
export async function withCache<T>(
  key: string,
  fn: () => Promise<T>,
  ttl: number = CACHE_TTL.SEARCH_RESULTS,
  options?: WithCacheOptions
): Promise<T> {
  // Skip cache if globally disabled or redis not configured
  if (DISABLE_CACHE || !redis) {
    return fn();
  }

  // Try to get from cache first
  const cached = await getCache<T>(key);
  if (cached !== null) {
    console.log(`✅ Cache HIT: ${key}`);
    return cached;
  }

  console.log(`❌ Cache MISS: ${key}`);

  // Single-flight: check if another caller is already computing this key
  const singleFlightEnabled = options?.singleFlight !== false; // default true
  if (singleFlightEnabled) {
    const inProgress = inFlight.get(key) as Promise<T> | undefined;
    if (inProgress) {
      return inProgress;
    }
  }

  // First caller (or single-flight disabled): execute and cache
  const promise = (async (): Promise<T> => {
    try {
      const result = await fn();
      await setCache(key, result, ttl);

      // Register key in prefix registry if specified
      if (options?.prefix) {
        await registerCacheKey(options.prefix, key);
      }

      return result;
    } finally {
      if (singleFlightEnabled) {
        inFlight.delete(key);
      }
    }
  })();

  if (singleFlightEnabled) {
    inFlight.set(key, promise);
  }

  return promise;
}
```

- [ ] **Step 4: Rodar teste para confirmar PASS**

Run: `npx vitest run lib/cache/__tests__/single-flight.test.ts`
Expected: PASS (1 test).

- [ ] **Step 5: Rodar testes existentes do redis-client para confirmar NO regression**

Run: `npx vitest run lib/cache/__tests__/redis-client.test.ts`
Expected: PASS (todos os testes existentes).

- [ ] **Step 6: TypeScript check**

Run: `npx tsc --noEmit 2>&1 | grep -E "lib/cache" || echo "no errors in scope"`
Expected: `no errors in scope`.

- [ ] **Step 7: Commit (TDD green)**

```bash
git add lib/cache/redis-client.ts
git -c user.email=danbarral@gmail.com -c user.name="Daniel Barral" commit -m "$(cat <<'EOF'
feat(cache): implement single-flight in withCache [Onda 4 PR 4.7]

Adiciona inFlight Map de módulo + lógica em withCache:
- Antes de executar fn(), verifica inFlight.get(key)
- Se há promise em andamento, retorna ela compartilhada
- try/finally garante delete na conclusão (sucesso ou erro)
- Default-on; singleFlight: false opt-out disponível

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: Teste stampede (3 callers concorrentes, fn chamado 1 vez)

**Files:**
- Modify: `lib/cache/__tests__/single-flight.test.ts`

- [ ] **Step 1: Adicionar teste de stampede**

Editar `lib/cache/__tests__/single-flight.test.ts`. Adicionar dentro do `describe('withCache single-flight', ...)`:

```ts
  it('de-duplica 3 callers concorrentes para a mesma key (fn chamado 1 vez)', async () => {
    let resolveFn: (value: number) => void = () => {};
    const fn = vi.fn(() => new Promise<number>((resolve) => {
      resolveFn = resolve;
    }));

    // Dispara 3 callers concorrentes
    const a = withCache('test:stampede', fn, 60);
    const b = withCache('test:stampede', fn, 60);
    const c = withCache('test:stampede', fn, 60);

    // Micro-yield para garantir que todas as 3 promises registraram
    await Promise.resolve();
    await Promise.resolve();

    // fn() ainda não resolveu — fn deve ter sido chamado apenas 1 vez
    expect(fn).toHaveBeenCalledTimes(1);

    // Resolve fn()
    resolveFn(42);

    // Todos os 3 awaiters recebem o mesmo valor
    expect(await a).toBe(42);
    expect(await b).toBe(42);
    expect(await c).toBe(42);

    // setCache chamado apenas 1 vez (única execução de fn)
    expect(mockSetex).toHaveBeenCalledTimes(1);
    expect(mockSetex).toHaveBeenCalledWith('test:stampede', 60, 42);

    // fn ainda só chamado 1 vez
    expect(fn).toHaveBeenCalledTimes(1);
  });
```

- [ ] **Step 2: Rodar teste para confirmar PASS**

Run: `npx vitest run lib/cache/__tests__/single-flight.test.ts`
Expected: PASS (2 tests).

- [ ] **Step 3: Commit**

```bash
git add lib/cache/__tests__/single-flight.test.ts
git -c user.email=danbarral@gmail.com -c user.name="Daniel Barral" commit -m "$(cat <<'EOF'
test(cache): cover stampede de-duplication (3 concurrent callers) [Onda 4 PR 4.7]

Asserta fn chamado exatamente 1 vez quando 3 callers concorrentes
disparam para a mesma key (regressão guard contra reintrodução de stampede).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: Teste erro em fn() compartilhada

**Files:**
- Modify: `lib/cache/__tests__/single-flight.test.ts`

- [ ] **Step 1: Adicionar teste de error path**

Editar `lib/cache/__tests__/single-flight.test.ts`. Adicionar dentro do `describe`:

```ts
  it('erro em fn() é propagado para todos os awaiters e não polui cache', async () => {
    const originalError = new Error('Gemini timeout');
    let rejectFn: (err: Error) => void = () => {};
    const fn = vi.fn(() => new Promise<number>((_resolve, reject) => {
      rejectFn = reject;
    }));

    // 2 callers concorrentes
    const a = withCache('test:error', fn, 60).catch((e) => e);
    const b = withCache('test:error', fn, 60).catch((e) => e);

    await Promise.resolve();
    await Promise.resolve();

    rejectFn(originalError);

    const resultA = await a;
    const resultB = await b;

    // Ambos recebem o mesmo erro original (mesma instância)
    expect(resultA).toBe(originalError);
    expect(resultB).toBe(originalError);

    // fn() chamado apenas 1 vez (single-flight ativo)
    expect(fn).toHaveBeenCalledTimes(1);

    // setCache NÃO foi chamado (erro impede)
    expect(mockSetex).not.toHaveBeenCalled();
  });
```

- [ ] **Step 2: Rodar teste**

Run: `npx vitest run lib/cache/__tests__/single-flight.test.ts`
Expected: PASS (3 tests).

- [ ] **Step 3: Commit**

```bash
git add lib/cache/__tests__/single-flight.test.ts
git -c user.email=danbarral@gmail.com -c user.name="Daniel Barral" commit -m "$(cat <<'EOF'
test(cache): cover shared-promise error propagation [Onda 4 PR 4.7]

Asserta que erro em fn() compartilhada propaga para todos os awaiters,
não chama setCache (sem cache poisoning), e libera o slot para próxima call.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: Teste re-execução pós-erro

**Files:**
- Modify: `lib/cache/__tests__/single-flight.test.ts`

- [ ] **Step 1: Adicionar teste**

Editar `lib/cache/__tests__/single-flight.test.ts`. Adicionar:

```ts
  it('após erro, próxima chamada com mesma key executa fn() fresh', async () => {
    // Primeira call: erro
    const failingFn = vi.fn(async () => {
      throw new Error('First call fails');
    });

    await expect(withCache('test:retry', failingFn, 60)).rejects.toThrow('First call fails');
    expect(failingFn).toHaveBeenCalledTimes(1);

    // Segunda call: sucesso (deve executar fresh)
    const successFn = vi.fn(async () => 100);
    const result = await withCache('test:retry', successFn, 60);

    expect(result).toBe(100);
    expect(successFn).toHaveBeenCalledTimes(1);
    expect(mockSetex).toHaveBeenCalledWith('test:retry', 60, 100);
  });
```

- [ ] **Step 2: Rodar teste**

Run: `npx vitest run lib/cache/__tests__/single-flight.test.ts`
Expected: PASS (4 tests).

- [ ] **Step 3: Commit**

```bash
git add lib/cache/__tests__/single-flight.test.ts
git -c user.email=danbarral@gmail.com -c user.name="Daniel Barral" commit -m "$(cat <<'EOF'
test(cache): cover re-execution after shared-promise rejection [Onda 4 PR 4.7]

Asserta que inFlight é limpo após erro e nova chamada à mesma key
executa fn() fresh sem reusar a promise rejeitada.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 7: Teste escape hatch (singleFlight: false)

**Files:**
- Modify: `lib/cache/__tests__/single-flight.test.ts`

- [ ] **Step 1: Adicionar teste**

Editar `lib/cache/__tests__/single-flight.test.ts`. Adicionar:

```ts
  it('singleFlight: false permite execução concorrente de fn() (escape hatch)', async () => {
    let count = 0;
    const fn = vi.fn(async () => {
      const myCount = ++count;
      // Yield para garantir intercalação
      await Promise.resolve();
      await Promise.resolve();
      return myCount;
    });

    // 3 callers concorrentes COM escape hatch
    const results = await Promise.all([
      withCache('test:no-sf', fn, 60, { singleFlight: false }),
      withCache('test:no-sf', fn, 60, { singleFlight: false }),
      withCache('test:no-sf', fn, 60, { singleFlight: false }),
    ]);

    // Sem single-flight: fn chamado 3x, cada um retorna seu próprio número
    expect(fn).toHaveBeenCalledTimes(3);
    expect(results).toEqual([1, 2, 3]);
  });
```

- [ ] **Step 2: Rodar teste**

Run: `npx vitest run lib/cache/__tests__/single-flight.test.ts`
Expected: PASS (5 tests).

- [ ] **Step 3: Commit**

```bash
git add lib/cache/__tests__/single-flight.test.ts
git -c user.email=danbarral@gmail.com -c user.name="Daniel Barral" commit -m "$(cat <<'EOF'
test(cache): cover singleFlight: false escape hatch [Onda 4 PR 4.7]

Asserta que opt-out funciona — sem single-flight, fn() roda N vezes
para N callers concorrentes (regressão para behavior original).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 8: Verificação final + push + abrir PR 4.7

**Files:**
- No code changes — final verification only

- [ ] **Step 1: Rodar suite completa**

Run: `npm run test 2>&1 | tail -15`
Expected: todos os testes existentes PASS + 5 novos em `single-flight.test.ts` PASS. Coverage threshold global pode falhar (pré-existente, padrão Onda 4 — não bloqueia).

- [ ] **Step 2: TypeScript check final**

Run: `npx tsc --noEmit 2>&1 | grep -E "lib/cache" || echo "no errors in scope"`
Expected: `no errors in scope`.

- [ ] **Step 3: Migration status**

Run: `npm run migration:api:status`
Expected: `0 legacy`.

- [ ] **Step 4: Verificar commits da branch**

Run: `git log main..HEAD --format='%h | A:%ae | %s'`
Expected: 6 commits (1 docs + 5 feat/test), todos com `A:danbarral@gmail.com`.

- [ ] **Step 5: Push branch**

```bash
git push -u origin refactor/cache-single-flight
```

- [ ] **Step 6: Criar PR via gh CLI**

```bash
gh pr create --title "feat(cache): single-flight in-memory em withCache [Onda 4 PR 4.7]" --body "$(cat <<'EOF'
## Summary

Adiciona single-flight de-duplication in-memory ao `withCache` em `lib/cache/redis-client.ts`. Quando 2+ callers ativam `withCache` para a mesma key durante um cache miss, apenas o primeiro executa `fn()`; os demais aguardam a mesma Promise e compartilham o resultado.

**Default-on automático nos 24 call sites.** Escape hatch `singleFlight: false` disponível (não usado em nenhum caller atual).

## Por quê

Cache stampede acontece quando múltiplos requests batem na mesma key durante miss — todos executam `fn()` em paralelo, desperdiçando custo computacional. Especialmente caro para chamadas LLM (`lib/ai/index.ts`, `lib/embeddings/vector-search.ts`, `app/api/documents/query/route.ts`).

## Auditoria de idempotência (no spec)

24/24 callers seguros para default-on:
- 19 read-only DB queries
- 3 com logging idempotente (logs ficam até mais limpos)
- 2 chamadas LLM caras (caso ideal pra single-flight)

## Implementação

- Constante de módulo `const inFlight = new Map<string, Promise<unknown>>()`
- `withCache` consulta `inFlight.get(key)` antes de executar `fn()`
- `try/finally` garante `inFlight.delete(key)` em sucesso ou erro
- Helper `__resetInFlightForTesting` exportado para vitest

## Test plan

- [x] 5 testes em `lib/cache/__tests__/single-flight.test.ts`: caller único, stampede 3 concorrentes, error path, re-execução pós-erro, escape hatch
- [x] Testes existentes em `redis-client.test.ts` continuam verdes (no regression)
- [x] `npx tsc --noEmit` clean em `lib/cache`
- [x] `npm run migration:api:status` retorna 0
- [ ] Pós-deploy: validação manual com tráfego sintético — disparar 2-3 requests concorrentes em `/api/documents/query` com mesma query, esperar 1 chamada Gemini em vez de N

## Itens fora do escopo

- **PR 4.7.1 (próxima):** Leak protection (`MAX_IN_FLIGHT = 1000` + sampled warning)
- **Distributed lock (SETNX):** se profiling pós-4.7 mostrar stampede cross-instance recorrente
- **Negative caching:** cachear erros transitórios para evitar retry hammering

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 7: Report PR URL + checks**

Run: `gh pr checks <PR_NUMBER>` (substituir pelo número retornado por gh pr create).
Expected: `Run Tests pending`, `Vercel pending` inicialmente.

**Wait for Vercel deploy preview to pass. Then merge via squash (Run Tests pode falhar por coverage threshold — padrão Onda 4):**

```bash
gh pr merge <PR_NUMBER> --squash
```

- [ ] **Step 8: Sync local main**

```bash
git checkout main
git pull origin main
```

Expected: novo commit da PR 4.7 mergeada no log.

---

## PR 4.7.1 — Leak protection

Branch: `feat/cache-leak-protection` (criada de main pós-merge da 4.7)

### Task 9: Criar branch + 1º teste failing (high-water-mark warning)

**Files:**
- Modify: `lib/cache/__tests__/single-flight.test.ts`

- [ ] **Step 1: Criar branch a partir de main atualizado**

```bash
git checkout main
git pull origin main
git checkout -b feat/cache-leak-protection
```

- [ ] **Step 2: Adicionar teste failing**

Editar `lib/cache/__tests__/single-flight.test.ts`. Adicionar dentro do mesmo `describe('withCache single-flight', ...)`:

```ts
  it('emite warning Sentry quando inFlight.size >= MAX_IN_FLIGHT (amostrado)', async () => {
    // Mock Math.random para forçar amostragem (sempre dispara o warning quando atingir threshold)
    const randomSpy = vi.spyOn(Math, 'random').mockReturnValue(0.005); // < 0.01

    const { apiLogger } = await import('@/lib/logger');
    const Sentry = await import('@sentry/nextjs');

    // Bloquear todas as fn() retornadas para encher o Map
    const blockedPromises: ((v: unknown) => void)[] = [];
    const fn = vi.fn(() => new Promise<number>((resolve) => {
      blockedPromises.push(resolve as (v: unknown) => void);
    }));

    // Encher inFlight com 1001 entries para cruzar o threshold
    const callers: Promise<unknown>[] = [];
    for (let i = 0; i < 1001; i++) {
      callers.push(withCache(`test:leak:${i}`, fn, 60).catch(() => null));
    }

    // Micro-yield para registrar promises
    await Promise.resolve();
    await Promise.resolve();

    // Warning Sentry deve ter sido chamado ao menos 1x (com Math.random < 0.01 forçado)
    expect(apiLogger.warn).toHaveBeenCalledWith(
      expect.objectContaining({
        cache_in_flight_size: expect.any(Number),
        max: 1000,
      }),
      'cache.single_flight.high_water_mark',
    );
    expect(Sentry.captureMessage).toHaveBeenCalledWith(
      'cache.in_flight.exceeded_threshold',
      expect.objectContaining({
        level: 'warning',
        extra: expect.objectContaining({ max: 1000 }),
      }),
    );

    // Resolver todas para limpar
    for (const resolve of blockedPromises) resolve(0);
    await Promise.all(callers);

    randomSpy.mockRestore();
  });
```

- [ ] **Step 3: Rodar teste — esperar FAIL (warning ainda não implementado)**

Run: `npx vitest run lib/cache/__tests__/single-flight.test.ts -t "high_water_mark"`
Expected: FAIL — `apiLogger.warn` não foi chamado com o payload esperado.

- [ ] **Step 4: Commit (TDD red)**

```bash
git add lib/cache/__tests__/single-flight.test.ts
git -c user.email=danbarral@gmail.com -c user.name="Daniel Barral" commit -m "$(cat <<'EOF'
test(cache): write failing test for high-water-mark warning [Onda 4 PR 4.7.1]

TDD red. Asserta que warning Sentry + apiLogger.warn disparam quando
inFlight cresce além de MAX_IN_FLIGHT, com sampling 1:100.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 10: Implementar MAX_IN_FLIGHT + registerInFlight helper

**Files:**
- Modify: `lib/cache/redis-client.ts`

- [ ] **Step 1: Importar Sentry no topo do arquivo (se ainda não importado)**

Editar `lib/cache/redis-client.ts`. Verificar se há `import * as Sentry from '@sentry/nextjs'` perto do topo. Se não houver, adicionar logo após o último `import` existente:

```ts
import * as Sentry from '@sentry/nextjs';
```

(Se já existir, pular este step.)

- [ ] **Step 2: Adicionar `MAX_IN_FLIGHT` + `registerInFlight` helper**

Localizar a seção `// Single-flight de-duplication (in-memory, per-process)` adicionada na PR 4.7 (logo antes de `WithCacheOptions`). Adicionar APÓS a declaração do `inFlight` Map e ANTES do `__resetInFlightForTesting`:

```ts
/**
 * Threshold informacional. Em condições normais `inFlight.size` é < 10.
 * Atingir este valor indica vazamento provável (try/finally falhou em algum lugar).
 * Não recusa novas entradas — apenas alerta com sampling 1:100 para não floodar.
 */
const MAX_IN_FLIGHT = 1000;

function registerInFlight<T>(key: string, promise: Promise<T>): void {
  if (inFlight.size >= MAX_IN_FLIGHT) {
    // Sampling 1:100 para evitar flood Sentry/logs em vazamento contínuo
    if (Math.random() < 0.01) {
      apiLogger.warn(
        { cache_in_flight_size: inFlight.size, max: MAX_IN_FLIGHT },
        'cache.single_flight.high_water_mark',
      );
      Sentry.captureMessage('cache.in_flight.exceeded_threshold', {
        level: 'warning',
        extra: { size: inFlight.size, max: MAX_IN_FLIGHT },
      });
    }
  }
  inFlight.set(key, promise);
}
```

- [ ] **Step 3: Trocar `inFlight.set(key, promise)` por `registerInFlight(key, promise)` em `withCache`**

Localizar dentro de `withCache` (no fim, logo antes do `return promise;`):

```ts
// ANTES:
if (singleFlightEnabled) {
  inFlight.set(key, promise);
}

return promise;
```

Substituir por:

```ts
// DEPOIS:
if (singleFlightEnabled) {
  registerInFlight(key, promise);
}

return promise;
```

- [ ] **Step 4: Rodar teste para confirmar PASS**

Run: `npx vitest run lib/cache/__tests__/single-flight.test.ts`
Expected: PASS (6 tests).

- [ ] **Step 5: TypeScript check**

Run: `npx tsc --noEmit 2>&1 | grep -E "lib/cache" || echo "no errors in scope"`
Expected: `no errors in scope`.

- [ ] **Step 6: Commit (TDD green)**

```bash
git add lib/cache/redis-client.ts
git -c user.email=danbarral@gmail.com -c user.name="Daniel Barral" commit -m "$(cat <<'EOF'
feat(cache): add leak protection (MAX_IN_FLIGHT + sampled warning) [Onda 4 PR 4.7.1]

Adiciona MAX_IN_FLIGHT = 1000 + helper registerInFlight com warning
amostrado 1:100 via apiLogger.warn + Sentry.captureMessage.

Não recusa novas entradas (correctness > defensive); apenas observabilidade
para detectar vazamento se algo escapar do try/finally.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 11: Verificação final + push + abrir PR 4.7.1

**Files:**
- No code changes — final verification only

- [ ] **Step 1: Rodar suite completa**

Run: `npm run test 2>&1 | tail -15`
Expected: todos PASS (6 testes em `single-flight.test.ts` + existentes).

- [ ] **Step 2: TypeScript + migration status**

Run:
```bash
npx tsc --noEmit 2>&1 | grep -E "lib/cache" || echo "no errors in scope"
npm run migration:api:status
```
Expected: clean + `0 legacy`.

- [ ] **Step 3: Verificar commits**

Run: `git log main..HEAD --format='%h | A:%ae | %s'`
Expected: 2 commits, ambos com `A:danbarral@gmail.com`.

- [ ] **Step 4: Push branch**

```bash
git push -u origin feat/cache-leak-protection
```

- [ ] **Step 5: Criar PR**

```bash
gh pr create --title "feat(cache): leak protection for in-flight map [Onda 4 PR 4.7.1]" --body "$(cat <<'EOF'
## Summary

Adiciona observabilidade de vazamento ao `inFlight` Map criado na PR 4.7 (#XXX).

- `MAX_IN_FLIGHT = 1000` — threshold informacional
- Helper `registerInFlight(key, promise)` emite warning quando `inFlight.size >= MAX_IN_FLIGHT`
- Sampling 1:100 (`Math.random() < 0.01`) para evitar flood
- `apiLogger.warn` + `Sentry.captureMessage` com payload `{ size, max }`

## Por quê

Em condições normais `inFlight.size` < 10. Atingir 1000 indica vazamento (try/finally falhou em algum lugar). Esta PR adiciona o sinal para detectar — **sem recusar** novas entradas (correctness > defensive coding).

## Decisões registradas no spec

- ❌ NÃO recusar entries quando exceder MAX — quebraria single-flight semanticamente
- ❌ NÃO usar LRU eviction — esconderia o bug
- ✅ Apenas alertar com sampling — sinal visível, sem flood

## Test plan

- [x] 1 teste em `lib/cache/__tests__/single-flight.test.ts` cobrindo: força 1001 entries, mock de Math.random < 0.01, verifica apiLogger.warn + Sentry.captureMessage chamados com payload esperado
- [x] 5 testes existentes da PR 4.7 continuam verdes (no regression)
- [x] `npx tsc --noEmit` clean em `lib/cache`

## Fecha Onda 4.7

Esta PR fecha a campanha 4.7 (2 PRs: 4.7 foundation + 4.7.1 leak protection). Próximos passos (fora desta onda):

- Monitorar Sentry breadcrumbs `cache.in_flight.exceeded_threshold` por ≥ 7 dias
- Monitorar logs Vercel `cache.single_flight.high_water_mark` — esperado zero entradas em condição normal
- Avaliar Onda 5 (podar features) e Onda 6 (refactor estrutural)

## Itens fora do escopo (documentados no spec)

- Distributed lock (Redis SETNX) — apenas se telemetria mostrar stampede cross-instance
- Negative caching — feature separada
- Métricas Sentry com cardinalidade alta (counts por key)

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 6: Aguardar CI + mergear**

Mesmo padrão da PR 4.7: aguardar Vercel deploy preview, depois `gh pr merge <NUMBER> --squash`.

- [ ] **Step 7: Sync local main**

```bash
git checkout main
git pull origin main
```

- [ ] **Step 8: Atualizar memória local (Controller — não Task de subagent)**

Editar `/Users/danba/.claude/projects/-Users-danba/memory/MEMORY.md`:

```markdown
- **Onda 4.7 completa** ✅ 2 PRs #XX-#XX (2026-05-XX) — single-flight in-memory em withCache + leak protection. 24 call sites herdam dedup automaticamente. Detalhes: [project_sessao_2026-05-XX-onda4.7.md](project_sessao_2026-05-XX-onda4.7.md).
```

Criar `/Users/danba/.claude/projects/-Users-danba/memory/project_sessao_2026-05-XX-onda4.7.md` com resumo da sessão (similar à da Onda 4.6).

---

## Self-review

### Spec coverage verificada

| Spec requirement | Task que cobre |
|---|---|
| Adiciona `inFlight` Map de módulo | Task 3 (PR 4.7) |
| Modifica `withCache` com single-flight | Task 3 |
| Estende `WithCacheOptions` com `singleFlight` | Task 3 |
| Exporta `__resetInFlightForTesting` | Task 3 |
| Default-on (singleFlight: true por omissão) | Task 3 (linha `singleFlightEnabled = options?.singleFlight !== false`) |
| Escape hatch `singleFlight: false` | Task 3 (lógica) + Task 7 (teste) |
| `try/finally` limpa Map em erro | Task 3 (lógica) + Tasks 5, 6 (testes) |
| Cobertura de teste: caller único | Task 2 |
| Cobertura de teste: stampede 3 concorrentes | Task 4 |
| Cobertura de teste: error path | Task 5 |
| Cobertura de teste: re-execução pós-erro | Task 6 |
| Cobertura de teste: escape hatch | Task 7 |
| Leak protection: `MAX_IN_FLIGHT = 1000` | Task 10 (PR 4.7.1) |
| Leak protection: sampled warning (1:100) | Task 10 |
| Leak protection: `apiLogger.warn` + `Sentry.captureMessage` | Task 10 |
| Cobertura de teste do leak | Task 9 (PR 4.7.1) |
| Mantém comportamento atual de `DISABLE_CACHE` e `!redis` | Task 3 (early return preservado) |

**100% spec coverage.** Sem gaps.

### Placeholder scan

Nenhum "TBD", "TODO", "fill in", "similar to Task X". Todos os steps com código mostram o código completo.

### Type consistency

- `inFlight: Map<string, Promise<unknown>>` — declarada Task 3, referenciada Tasks 9, 10
- `MAX_IN_FLIGHT = 1000` — Task 10
- `registerInFlight<T>(key, promise)` — Task 10
- `__resetInFlightForTesting(): void` — Task 3, usada em todos os testes via `beforeEach`
- `singleFlight?: boolean` — Task 3 (interface), Task 7 (teste)
- Mensagens de log:
  - `'cache.single_flight.high_water_mark'` — Task 10 (impl) + Task 9 (teste)
  - `'cache.in_flight.exceeded_threshold'` — Task 10 (impl) + Task 9 (teste)
  - Idênticas entre teste e implementação ✅

---

## Execução

Plan complete and saved to `docs/superpowers/plans/2026-05-18-onda4.7-single-flight-cache.md`. Two execution options:

**1. Subagent-Driven (recommended)** — dispatcho um subagent fresco por task, revisão entre tasks, iteração rápida. Bom porque as 11 tasks são bem isoladas.

**2. Inline Execution** — executo tudo nesta sessão usando `executing-plans`, batch com checkpoints.

**Qual abordagem?**
