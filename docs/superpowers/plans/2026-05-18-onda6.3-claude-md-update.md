# Onda 6.3 — Atualizar CLAUDE.md Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Atualizar `CLAUDE.md` em 9 áreas defasadas + 4 adições de features (Ondas 4.5, 4.6, 4.7, 5).

**Architecture:** 1 PR única editando 1 arquivo no root (`CLAUDE.md`). Sem código de produção tocado. Validação por grep + visual inspect.

**Tech Stack:** Markdown puro. Nenhuma dependência.

**Spec:** [docs/superpowers/specs/2026-05-18-onda6.3-claude-md-update-design.md](../specs/2026-05-18-onda6.3-claude-md-update-design.md)

---

## File Structure

**Arquivo modificado:** `CLAUDE.md` no root (757 LOC atual; ~830-840 LOC esperado pós-fix)

---

## Convenções

- **Path:** comandos rodam de `/Users/danba/Site do Barral/sitedobarral/`
- **Git author/committer:** SEMPRE Daniel Barral <danbarral@gmail.com>:
  ```bash
  git -c user.email=danbarral@gmail.com -c user.name="Daniel Barral" commit -m "..."
  ```
  Sem modificar git config. Sem push até task de push explícita.
- **Branch:** `docs/claude-md-onda5-update`
- **Commit message:** `docs(claude-md): atualizar com Ondas 4.5-5 + fixes Mac/auto-deploy [Onda 6 PR 6.3]`

---

## Task 1: Criar branch + commit do spec/plan

- [ ] **Step 1: Garantir main atualizado**

```bash
git status --short
git checkout main 2>/dev/null
git pull origin main
```

Expected: main na ponta; 2 docs untracked (`docs/superpowers/specs/2026-05-18-onda6.3-claude-md-update-design.md`, `docs/superpowers/plans/2026-05-18-onda6.3-claude-md-update.md`).

- [ ] **Step 2: Criar branch**

```bash
git checkout -b docs/claude-md-onda5-update
```

- [ ] **Step 3: Commit dos docs (spec + plan)**

```bash
git add docs/superpowers/specs/2026-05-18-onda6.3-claude-md-update-design.md docs/superpowers/plans/2026-05-18-onda6.3-claude-md-update.md
git -c user.email=danbarral@gmail.com -c user.name="Daniel Barral" commit -m "$(cat <<'EOF'
docs(onda6.3): spec + plan da atualização do CLAUDE.md [Onda 6 PR 6.3]

Foundation: spec + plan pra trazer CLAUDE.md ao estado atual
pós-campanha de saneamento (Ondas 4.5, 4.6, 4.7, 5).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 4: Verificar autoria**

```bash
git log -1 --format='%h | A:%ae | C:%ce | %s'
```

Expected: `A:danbarral@gmail.com | C:danbarral@gmail.com`.

---

## Task 2: Atualizar CLAUDE.md — Working Directory + paths Mac (linha 7, 745-754)

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: Atualizar linha 7 (Working Directory)**

Localizar:
```markdown
1. **Working Directory:** SEMPRE rodar comandos de `C:\Users\User\projetos\sitedobarral\` (Windows desktop, desde 2026-05-08). Path antigo `/Users/danba/Site do Barral/sitedobarral/` (MacBook) está obsoleto.
```

Substituir por:
```markdown
1. **Working Directory:** SEMPRE rodar comandos de `/Users/danba/Site do Barral/sitedobarral/` (MacBook). Path Windows mencionado em versões anteriores (`C:\Users\User\projetos\sitedobarral\`) foi descontinuado.
```

- [ ] **Step 2: Atualizar linhas 745-754 (First Time Setup Windows → Mac)**

Localizar bloco completo:
```markdown
**First Time Setup (Windows, ambiente atual):**

\```powershell
cd C:\Users\User\projetos\sitedobarral
npm install
copy .env.example .env.local  # Editar com seus valores
npx prisma generate; npx prisma db push
node scripts/create-admin.js admin@email.com password123 "Admin Name"
npm run dev
\```
```

Substituir por:
```markdown
**First Time Setup (Mac, ambiente atual):**

\```bash
cd "/Users/danba/Site do Barral/sitedobarral"
npm install
cp .env.example .env.local  # Editar com seus valores
npx prisma generate && npx prisma db push
node scripts/create-admin.js admin@email.com password123 "Admin Name"
npm run dev
\```
```

(Substitua os ``` por triple-backtick reais — aqui escapado pra evitar conflito com este plan markdown.)

- [ ] **Step 3: Verificação**

```bash
grep -n "C:\\\\Users\\\\User\|powershell" CLAUDE.md || echo "no Windows refs"
```

Expected: `no Windows refs`.

---

## Task 3: Atualizar CLAUDE.md — Models count (linha 72)

- [ ] **Step 1: Atualizar contagem de models**

Localizar:
```markdown
- `prisma/schema.prisma` - Database schema (26 models)
```

Substituir por:
```markdown
- `prisma/schema.prisma` - Database schema (70 models)
```

- [ ] **Step 2: Verificar**

```bash
grep -n "26 models\|70 models" CLAUDE.md
```

Expected: apenas o "70 models" deve aparecer.

---

## Task 4: Atualizar CLAUDE.md — Tests count (linha 585)

- [ ] **Step 1: Atualizar Fase 9 stats**

Localizar:
```markdown
- Fase 9: Automated testing (Vitest, 577 tests, 84%+ coverage)
```

Substituir por:
```markdown
- Fase 9: Automated testing (Vitest 4, **1473+ tests**, ~73% global coverage — threshold global de 80% é pré-existente da campanha, não bloqueia merges com `--squash` direto)
```

- [ ] **Step 2: Verificar**

```bash
grep -n "577 tests\|1473" CLAUDE.md
```

Expected: apenas "1473" deve aparecer.

---

## Task 5: Atualizar CLAUDE.md — Remover linha do DOU_CLIPPING_V2_ENABLED (linha 595)

- [ ] **Step 1: Remover linha**

Localizar a linha exata:
```markdown
- DOU Clipping v2 (atrás do flag `DOU_CLIPPING_V2_ENABLED`)
```

DELETAR completamente a linha (sem substituir — a feature continua viva, mas não está mais "atrás de flag").

- [ ] **Step 2: Verificar**

```bash
grep -n "DOU_CLIPPING_V2_ENABLED" CLAUDE.md || echo "fully removed"
```

Expected: `fully removed`.

---

## Task 6: Atualizar CLAUDE.md — Deploy Vercel (linha 756)

- [ ] **Step 1: Atualizar linha final**

Localizar:
```markdown
**Workflow git:** trunk-based em `main` (branch `develop` deletada em abr/2026). Ritmo atual: ~13 commits/dia. Deploy Vercel é manual (`vercel --prod`) — GitHub não está conectado ao projeto Vercel.
```

Substituir por:
```markdown
**Workflow git:** trunk-based em `main` (branch `develop` deletada em abr/2026). Ritmo atual: ~13 commits/dia. **Auto-deploy ATIVO** (GitHub conectado ao projeto Vercel, ~4min build/deploy típico). PRs aprovadas e merged em main disparam deploy automaticamente.
```

- [ ] **Step 2: Verificar**

```bash
grep -n "Deploy Vercel é manual\|GitHub não está conectado" CLAUDE.md || echo "fully removed"
```

Expected: `fully removed`.

---

## Task 7: Atualizar CLAUDE.md — Adicionar `lib/lms/` em Project Structure (linhas 68-74)

- [ ] **Step 1: Adicionar bullet de `lib/lms/`**

Localizar o bloco "Structure:" (linhas 66-74). Após a linha `- lib/agu-modules/ - AGU scrapers (ONs, Pareceres, DECOR, Súmulas)`, adicionar uma nova linha:

```markdown
- `lib/lms/` - Helpers LMS analytics (query-timing, analytics-queries, progress-aggregation) — criado na Onda 4.6
```

- [ ] **Step 2: Verificar**

```bash
grep -n "lib/lms/" CLAUDE.md
```

Expected: ao menos 1 hit (a linha adicionada).

---

## Task 8: Atualizar CLAUDE.md — Adicionar 4 entradas de Recent Features

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: Localizar topo da seção "✅ Completed" (linha ~576)**

Após a linha `**✅ Completed:**`, ANTES de `- Auth (JWT, QR codes, ...)`, adicionar 4 entradas no topo da lista:

```markdown
- **Onda 4.5 — JSON → String[] nativo PG (2026-05-17/18):** `leiArticles` migrado para `leiArticlesArr String[]` em 11 modelos Prisma + 7 GIN indexes. 5x speedup medido em prod. 9 PRs #72-#80. Drop coluna legada programado pra ~2026-06-01.
- **Onda 4.6 — Performance LMS analytics (2026-05-18):** `lib/lms/` subdir com 3 arquivos (`query-timing.ts`, `analytics-queries.ts`, `progress-aggregation.ts`) + 6 helpers. N+1 eliminado em 4 endpoints LMS. 3 PRs #82-#84.
- **Onda 4.7 — Single-flight em `withCache` (2026-05-18):** Dedup in-memory de chamadas concorrentes via `Map<key, Promise>` em `lib/cache/redis-client.ts`. 24 call sites herdam automaticamente. Leak protection `MAX_IN_FLIGHT=1000`. 2 PRs #85-#86.
- **Onda 5 — Podar features mortas (2026-05-18):** ~1500 LOC removidas. Flag `DOU_CLIPPING_V2_ENABLED` + 440 LOC legacy dropadas, pages AGU admin duplicadas consolidadas (545 LOC), cron migrado de `agu-scraper.ts` v1 → v4 e v1 dropado (496 LOC). 3 PRs #87-#89.
```

- [ ] **Step 2: Verificar**

```bash
grep -n "Onda 4.5\|Onda 4.6\|Onda 4.7\|Onda 5 —" CLAUDE.md
```

Expected: 4 hits (as 4 entradas adicionadas).

---

## Task 9: Atualizar CLAUDE.md — Adicionar padrão Single-flight em Architecture Patterns

- [ ] **Step 1: Localizar fim da seção "Embeddings/pgvector Pipeline" (~linha 654)**

Após `- 📖 Ver lib/embeddings/document-processor.ts...` e antes de `### Atos Legislativos — Embeddings Separados`, adicionar:

```markdown

### Single-flight em `withCache` (Onda 4.7)
1. `withCache(key, fn, ttl, options)` consulta `inFlight: Map<string, Promise<unknown>>` antes de executar `fn()`
2. Se já há promise registrada para a key: retorna ela compartilhada (todos callers concorrentes recebem mesmo resultado)
3. `try/finally` garante `inFlight.delete(key)` em sucesso OU erro — sem cache poisoning
4. Default-on; `options.singleFlight: false` opt-out disponível (nenhum caller atual usa)
5. Leak protection: warning amostrado 1:100 quando `inFlight.size >= 1000`
- 📖 Ver `lib/cache/redis-client.ts`, `lib/cache/__tests__/single-flight.test.ts`
```

- [ ] **Step 2: Verificar**

```bash
grep -n "Single-flight em" CLAUDE.md
```

Expected: 1 hit.

---

## Task 10: Atualizar CLAUDE.md — Notes for Future Claude Instances

- [ ] **Step 1: Adicionar 1 ponto na seção "Notes" (linha ~720)**

Após `- Manter tom formal e profissional (contexto jurídico)`, adicionar:

```markdown
- **Campanha de saneamento 2026-05:** 60 PRs em 6 ondas. Padrões consolidados: `withAdminApi`/`withUserApi`/`withPublicApi` em rotas (Onda 4), `lib/ai/index.ts` `generate()` como porta única para LLM (Onda 4.4), `lib/lms/` helpers (Onda 4.6), single-flight em `withCache` (Onda 4.7), `leiArticlesArr` array nativo (Onda 4.5). Antes de adicionar nova rota/feature, verificar se existe helper consolidado.
```

- [ ] **Step 2: Verificar**

```bash
grep -n "Campanha de saneamento 2026-05" CLAUDE.md
```

Expected: 1 hit.

---

## Task 11: Smoke check + sanity verifications

- [ ] **Step 1: Verificar tamanho final do arquivo**

```bash
wc -l CLAUDE.md
```

Expected: ~830-840 LOC (up from 757).

- [ ] **Step 2: Grep exhaustivo das defasagens conhecidas**

```bash
echo "--- Windows working dir ---"
grep -n "C:\\\\Users\\\\User\|powershell" CLAUDE.md || echo "OK"

echo "--- Old DOU flag ---"
grep -n "DOU_CLIPPING_V2_ENABLED" CLAUDE.md || echo "OK"

echo "--- Old models count ---"
grep -n "26 models" CLAUDE.md || echo "OK"

echo "--- Old tests count ---"
grep -n "577 tests" CLAUDE.md || echo "OK"

echo "--- Old deploy claim ---"
grep -n "Deploy Vercel é manual\|GitHub não está conectado" CLAUDE.md || echo "OK"
```

All grep outputs should report `OK` (no matches).

- [ ] **Step 3: Visual inspect**

```bash
head -20 CLAUDE.md
sed -n '575,610p' CLAUDE.md
```

Inspect the 4 new Onda entries appear at TOP of "✅ Completed" section, formatting consistent with siblings.

- [ ] **Step 4: TypeScript check (sanity — should be untouched)**

```bash
npx tsc --noEmit 2>&1 | head -5
```

Expected: same as before (CLAUDE.md doesn't affect TS).

- [ ] **Step 5: Run tests (sanity — should be untouched)**

```bash
npm run test 2>&1 | tail -5
```

Expected: same 1473 tests pass.

- [ ] **Step 6: Commit**

```bash
git add CLAUDE.md
git -c user.email=danbarral@gmail.com -c user.name="Daniel Barral" commit -m "$(cat <<'EOF'
docs(claude-md): atualizar com Ondas 4.5-5 + fixes Mac/auto-deploy [Onda 6 PR 6.3]

9 correções e 4 adições:

Correções:
- Working Directory: Mac (não Windows)
- Database models: 70 (não 26)
- Tests count: 1473+ (não 577)
- Remove linha DOU_CLIPPING_V2_ENABLED (flag dropada na PR #87)
- First Time Setup: bash/zsh (não PowerShell)
- Deploy Vercel: auto-deploy ATIVO via GitHub (não manual)

Adições:
- lib/lms/ em Project Structure
- 4 entradas em "✅ Completed": Ondas 4.5, 4.6, 4.7, 5
- Padrão Single-flight em Architecture Patterns
- Nota sobre campanha de saneamento 2026-05 em "Notes for Future Claude"

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 12: Push + abrir PR + mergear + atualizar memória

- [ ] **Step 1: Verificar commits**

```bash
git log main..HEAD --format='%h | A:%ae | C:%ce | %s'
```

Expected: 2 commits (docs + claude-md update), ambos `danbarral@gmail.com`.

- [ ] **Step 2: Push**

```bash
git push -u origin docs/claude-md-onda5-update
```

- [ ] **Step 3: Criar PR**

```bash
gh pr create --title "docs(claude-md): atualizar com Ondas 4.5-5 + fixes Mac/auto-deploy [Onda 6 PR 6.3]" --body "$(cat <<'EOF'
## Summary

Traz o `CLAUDE.md` ao estado atual pós-campanha de saneamento 2026-05 (Ondas 4.5, 4.6, 4.7, 5).

## Correções (6 defasagens)

| Defasagem | Antes | Depois |
|---|---|---|
| Working Directory | `C:\Users\User\projetos\sitedobarral\` (Windows) | `/Users/danba/Site do Barral/sitedobarral/` (Mac) |
| Database models | "26 models" | "70 models" |
| Tests count | "577 tests, 84% coverage" | "1473+ tests, ~73% global" |
| DOU flag | "DOU Clipping v2 (atrás do flag `DOU_CLIPPING_V2_ENABLED`)" | Removido — flag dropada na PR #87 |
| First Time Setup | PowerShell + Windows paths | bash/zsh + Mac paths |
| Deploy Vercel | "Manual / GitHub não conectado" | "**Auto-deploy ATIVO** via GitHub, ~4min" |

## Adições (4 frentes)

- **Project Structure:** menciona `lib/lms/` (criado na Onda 4.6)
- **"✅ Completed":** 4 novas entradas no topo da seção (Ondas 4.5, 4.6, 4.7, 5)
- **Architecture Patterns:** novo bloco "Single-flight em `withCache` (Onda 4.7)"
- **Notes for Future Claude:** nota sobre campanha de saneamento 2026-05 com padrões consolidados

## Test plan

- [x] `grep` exhaustivo das defasagens conhecidas retorna zero hits
- [x] `wc -l` mostra crescimento esperado (~80 LOC)
- [x] `npm run test` continua passando (CLAUDE.md não afeta runtime)
- [x] Visual inspect das seções modificadas
- [x] Markdown rendering íntegro (sem links/tabelas quebradas)

## Fecha PR 6.3 da Onda 6

Esta é a única PR da Onda 6 nesta sessão. Próximas (sessões dedicadas):
- **PR 6.1** — Quebrar componentes >800 LOC (10 candidatos identificados, 1-2 semanas)
- **PR 6.2** — Server Components migration (65 client pages, triagem ~1 dia)

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 4: Aguardar Vercel deploy + mergear**

```bash
gh pr merge <PR_NUMBER> --squash
git checkout main
git pull origin main
```

- [ ] **Step 5: Atualizar memória (Controller — não Task de subagent)**

Editar `/Users/danba/.claude/projects/-Users-danba/memory/MEMORY.md`:

```markdown
- **Onda 6.3 completa** ✅ 1 PR #XX (2026-05-18) — atualização do CLAUDE.md com Ondas 4.5-5 + fixes Mac/auto-deploy. 9 correções (Working Directory Windows→Mac, models 26→70, tests 577→1473, DOU flag removida, Setup PowerShell→bash, Deploy "manual"→"auto-deploy ativo"); 4 adições (lib/lms/ em Structure, 4 entradas Recent Features, padrão Single-flight, nota campanha saneamento). PRs 6.1 (quebrar componentes >800 LOC, 10 candidatos) e 6.2 (Server Components, 65 client pages) ficam pra sessões dedicadas.
```

Criar `/Users/danba/.claude/projects/-Users-danba/memory/project_sessao_2026-05-18-onda6.3.md` com resumo similar.

---

## Self-review

### Spec coverage

| Spec item | Task |
|---|---|
| Linha 7 (Working Directory) | Task 2 Step 1 |
| Linha 72 (models count) | Task 3 |
| Linha 585 (tests count) | Task 4 |
| Linha 595 (DOU flag) | Task 5 |
| Linha 745-754 (Windows Setup) | Task 2 Step 2 |
| Linha 756 (Deploy Vercel) | Task 6 |
| Adicionar `lib/lms/` em Structure | Task 7 |
| 4 entradas em "✅ Completed" | Task 8 |
| Padrão Single-flight em Architecture | Task 9 |
| Nota campanha em "Notes" | Task 10 |

**100% spec coverage.**

### Placeholder scan

Sem "TBD", "TODO", "fill in", "similar to Task X". Todas as substituições têm conteúdo completo.

### Type consistency

N/A — PR de documentação Markdown.

---

## Execução

Plan complete and saved to `docs/superpowers/plans/2026-05-18-onda6.3-claude-md-update.md`. Two execution options:

**1. Subagent-Driven (recommended)** — dispatcho um subagent fresco por task, revisão entre tasks. 12 tasks mas a maioria são edits de 1 linha — execução rápida.

**2. Inline Execution** — executo tudo nesta sessão com checkpoints.

**Qual abordagem?**
