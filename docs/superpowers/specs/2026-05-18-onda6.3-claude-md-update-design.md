# Onda 6.3 — Atualizar CLAUDE.md

**Data:** 2026-05-18
**Status:** Aprovado, implementação pendente
**Onda:** 6.3 (Saneamento 2026-05 — única frente desta sessão da Onda 6)
**Escopo:** 1 PR única atualizando o `CLAUDE.md` no root do repositório

---

## Contexto

`CLAUDE.md` (757 LOC) é o guia consultado por toda sessão futura do Claude Code neste repositório. Após 7 ondas de saneamento na sessão 2026-05-18 (Ondas 4.5, 4.6, 4.7, 5), múltiplas afirmações no arquivo estão defasadas. Esta PR corrige sem refatorar o conteúdo válido — mudanças cirúrgicas em 9 áreas + 4 adições de features.

A Onda 6 inteira (PR 6.1 quebrar componentes >800 LOC, PR 6.2 Server Components migration, PR 6.3 docs) é estimada em 2-4 semanas. Esta sessão entrega **apenas PR 6.3** porque é a única que cabe num bloco de ~3-4h. PR 6.1 e 6.2 ficam para sessões dedicadas futuras.

## Áreas a corrigir

| # | Linha(s) | Defasagem atual | Correção |
|---|---|---|---|
| 1 | 7 | "Working Directory: SEMPRE rodar de `C:\Users\User\projetos\sitedobarral\` (Windows)" | Substituir pelo path Mac: `/Users/danba/Site do Barral/sitedobarral/` |
| 2 | 72 | "Database schema (26 models)" | Atualizar pra **70 models** (fonte: schema.prisma) |
| 3 | 585 | "Fase 9: Automated testing (Vitest, 577 tests, 84%+ coverage)" | Atualizar pra **1473+ tests; coverage global ~73%** com nota sobre threshold pré-existente |
| 4 | 595 | "DOU Clipping v2 (atrás do flag `DOU_CLIPPING_V2_ENABLED`)" | Remover linha — flag dropada na PR #87 (Onda 5.1) |
| 5 | 745-754 | Seção "First Time Setup (Windows, ambiente atual)" com PowerShell | Substituir por bash/zsh (Mac) com paths atualizados |
| 6 | 756 | "Deploy Vercel é manual (`vercel --prod`) — GitHub não está conectado" | Atualizar: **Auto-deploy ATIVO (GitHub conectado)**, ~4min build típico |

## Áreas a adicionar

### Project Structure (linhas 64-74)
Adicionar `lib/lms/` na lista (subdir criado na Onda 4.6 com `query-timing.ts`, `analytics-queries.ts`, `progress-aggregation.ts`).

### Recent Features (seção ~568-604)
Adicionar 4 entradas no topo da seção "✅ Completed":

```markdown
- **Onda 4.5 — JSON → String[] nativo PG (2026-05-17/18):** `leiArticles` migrado para `leiArticlesArr String[]` em 11 modelos Prisma + 7 GIN indexes. 5x speedup medido em prod. 9 PRs #72-#80. Drop coluna legada programado pra ~2026-06-01.
- **Onda 4.6 — Performance LMS analytics (2026-05-18):** `lib/lms/` subdir com 3 arquivos (`query-timing.ts`, `analytics-queries.ts`, `progress-aggregation.ts`) + 6 helpers. N+1 eliminado em 4 endpoints LMS. 3 PRs #82-#84.
- **Onda 4.7 — Single-flight em `withCache` (2026-05-18):** Dedup in-memory de chamadas concorrentes via `Map<key, Promise>` em `lib/cache/redis-client.ts`. 24 call sites herdam automaticamente. Leak protection `MAX_IN_FLIGHT=1000`. 2 PRs #85-#86.
- **Onda 5 — Podar features mortas (2026-05-18):** ~1500 LOC removidas. Flag `DOU_CLIPPING_V2_ENABLED` + 440 LOC legacy dropadas, pages AGU admin duplicadas consolidadas (545 LOC removidas), cron migrado de `agu-scraper.ts` v1 → v4 e v1 dropado (496 LOC). 3 PRs #87-#89.
```

### Important Architecture Patterns (após linha ~659 "Embeddings/pgvector Pipeline")
Adicionar 1 padrão novo:

```markdown
### Single-flight em `withCache` (Onda 4.7)
1. `withCache(key, fn, ttl, options)` consulta `inFlight: Map<string, Promise<unknown>>` antes de executar `fn()`
2. Se já há promise registrada para a key: retorna ela compartilhada (todos callers concorrentes recebem mesmo resultado)
3. `try/finally` garante `inFlight.delete(key)` em sucesso OU erro — sem cache poisoning
4. Default-on; `options.singleFlight: false` opt-out disponível (nenhum caller atual usa)
5. Leak protection: warning amostrado 1:100 quando `inFlight.size >= 1000`
- 📖 Ver `lib/cache/redis-client.ts`, `lib/cache/__tests__/single-flight.test.ts`
```

### Notes for Future Claude Instances (linha 720-725)
Adicionar 1 ponto:

```markdown
- **Campanha de saneamento 2026-05:** 60 PRs em 6 ondas. Padrões consolidados: `withAdminApi`/`withUserApi`/`withPublicApi` em rotas (Onda 4), `lib/ai/index.ts` `generate()` como porta única para LLM (Onda 4.4), `lib/lms/` helpers (Onda 4.6), single-flight em `withCache` (Onda 4.7), `leiArticlesArr` array nativo (Onda 4.5). Antes de adicionar nova rota/feature, verificar se existe helper consolidado.
```

## Arquitetura

**1 arquivo modificado:** `CLAUDE.md` no root.

**Sem outras mudanças.** Não há código de produção tocado.

## Testing

**Não há testes pra PR de docs.** Validação:
- `wc -l CLAUDE.md` antes/depois (deve crescer ~50-80 LOC líquidas)
- `grep -n "DOU_CLIPPING_V2_ENABLED\|C:\\\\Users\|26 models\|577 tests" CLAUDE.md` deve retornar zero hits pós-fix
- Smoke test: ler `CLAUDE.md` topo-bottom em ~1 min e ver se faz sentido

## Critério de "PR 6.3 fechada"

- 1 PR mergeada em main
- Run Tests verde (com possível fail de coverage threshold global, padrão)
- `npm run migration:api:status` = 0 (sanity)
- Grep das defasagens listadas retorna zero hits

## Itens conscientemente fora do escopo

- **PR 6.1 (quebrar componentes >800 LOC)** — 10 componentes identificados (lei-comentada 1608 LOC, dou-filtros 1330 LOC, etc). Estimado 2-4 semanas. Sessões dedicadas.
- **PR 6.2 (Server Components migration)** — 65 pages com `'use client'`. Triagem RSC-friendly necessária. Estimado 3-4 dias. Sessão dedicada.
- **Auditoria do CLAUDE.md além das 9 áreas conhecidas** — escopo controlado pra não virar yak-shaving. Outras defasagens menores ficam pra Onda 7 ou ad-hoc.

## Riscos identificados

| Risco | Mitigação |
|---|---|
| Esquecer alguma defasagem | Self-review pós-implementação com grep exhaustivo das frases obsoletas conhecidas |
| Quebrar markdown rendering (links, tabelas) | Visual inspect do diff antes do push |
| Tom inconsistente entre seções novas e existentes | Manter tom técnico/conciso como o restante do arquivo |

## Referências

- [project_plano_saneamento.md](../../memory/project_plano_saneamento.md)
- [project_sessao_2026-05-18-onda5.md](../../memory/project_sessao_2026-05-18-onda5.md)
- [project_sessao_2026-05-18-onda4.7.md](../../memory/project_sessao_2026-05-18-onda4.7.md)
- [project_sessao_2026-05-18-onda4.6.md](../../memory/project_sessao_2026-05-18-onda4.6.md)
- [feedback_pre_launch_audacious.md](../../memory/feedback_pre_launch_audacious.md)
